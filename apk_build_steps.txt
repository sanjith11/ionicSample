

set path first:

export ANDROID_HOME=/home/sanjith/Android/Sdk
PATH=$PATH:$ANDROID_HOME/tools
PATH=$PATH:$ANDROID_HOME/platform-tools		

build command:
sudo ionic cordova build android --prod --release


        cordova build android --release
